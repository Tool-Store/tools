This is an existing active project. Adapt it to the Vibe Code Template workflow.

1. Read the existing project documentation and key config files to understand the product, users, workflows, technical stack, and constraints.
2. Read the repository structure and key implementation entrypoints, then create or update `docs/ARCHITECTURE_INDEX.md` to reflect the real architecture, ownership boundaries, and where changes belong.
3. Fill in as much of `docs/PROJECT.md` as possible from the existing docs, code, routes, APIs, data model, and user flows. Replace placeholders when the answer is supported by evidence in the project.
4. Do not invent missing requirements. If something cannot be determined from the project, leave it for the user and track it clearly.
5. Update the target project's `README.md` to tell new contributors that the project uses the Vibe Code Template and link them to `https://github.com/BertoMejia/Vibe-Code-Template` for usage instructions.
6. At the end, provide a concise report of what information is still missing in `docs/PROJECT.md`, grouped by section, so the user can finish it.
