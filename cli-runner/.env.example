# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# MCP bind (used when not running stdio)
MCP_HOST=127.0.0.1
MCP_PORT=8000
MCP_TRANSPORT=stdio

# Where install state is stored inside the container
CLI_RUNNER_STATE_PATH=/app/data/installed.json
